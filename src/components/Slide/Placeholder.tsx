import { FC } from 'react';
import Placeholder from 'rsuite/Placeholder';

const SlidePlaceholder: FC = () => {
  return <Placeholder.Graph active style={{ height: '100%' }} />;
};

export default SlidePlaceholder;
